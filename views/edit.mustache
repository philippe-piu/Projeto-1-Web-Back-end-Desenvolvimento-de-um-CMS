<!doctype html>
<html lang="pt">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="/styles/edit/style.css" rel="stylesheet">
    <title>+Esportes Editar Noticia</title>
  </head>
  <body class="container">
    <header class="header">
      <div class="nav-container">
        <div class="logo">
          <h2>Blog</h2>
          <h1>+Esportes</h1>
        </div>
        <a href="/" class="button-entrar">
          <img src="/assets/voltar.png" alt="">
          Sair
        </a>       
      </div>
    </header>

    <main class="main-container">
      <div class="criar-noticia-container">
        <form class="form-noticia" action="/editar/{{id}}" method="POST" enctype="multipart/form-data">
          <div class="header-form">
            <h1>Edite sua notícia esportiva</h1>
            <h2>Altere as informações abaixo que desejar para alterar a notícia!</h2>
          </div>

          <div class="main-form">
            <div class="cabecalho-container">
              <div class="input-tittle-container">
                 <label for="titulo">Titulo (o titulo da noticia será a url da sua pagina):</label>
                <input type="text" name="titulo" id="titulo" value="{{titulo}}"> <!-- Verifique se "{{titulo}}" está correto -->
              </div>
              <div class="input-tema-container">
                <label for="tema">Tema:</label>
                <input type="text" name="categoria" id="categoria" value="{{categoria}}"> <!-- Verifique se "{{categoria}}" está correto -->
              </div>
            </div>

            <div class="input-conteudo-container">
              <textarea class="input-noticia" name="conteudo" id="conteudo">{{conteudo}}</textarea> <!-- Verifique se "{{conteudo}}" está correto -->
            </div>

            <div class="input-resumo-container">
              <label for="resumo">Resumo:</label>
              <textarea name="resumo" id="resumo" maxlength="200">{{resumo}}</textarea> <!-- Verifique se "{{resumo}}" está correto -->
            </div>

            <div class="input-image-container">
              <label for="imagem">Escolha a capa da noticia: </label>
              <input type="file" name="image" id="image" value="Selecionar imagem">
            </div>
            
            <div class="rodape-container">
              <div class="input-autor-container">
                <label for="autor">Autor:</label>
                <input type="text" name="autor" id="autor" value="{{autor}}"> <!-- Verifique se "{{autor}}" está correto -->
              </div>
              <div class="input-data-container">
                <label for="data">Data de publicação:</label>
                <input type="date" name="data" id="data" value="{{data}}"> <!-- Verifique se "{{data}}" está correto -->
              </div>
            </div>
          </div>
          <div class="button-publicar">
            <button type="submit">Editar</button>
          </div>
        </form>
      </div>
    </main>
  </body>
</html>
